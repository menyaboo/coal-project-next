import type { FC } from 'react'
import {MainPicture, ProductionList} from "@/widgets/main/components";

const MainPage: FC = () => (
  <main>
    <MainPicture />
    <ProductionList />
  </main>
)

export default MainPage
